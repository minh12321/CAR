import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'car-optimization-engine',
  webDir: 'dist'
};

export default config;
